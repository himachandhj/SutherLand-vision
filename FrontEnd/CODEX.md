# Agent Instructions for this Project

- **NEVER** search, read, or modify the `node_modules/` directory.
- **NEVER** read `.env` files or secret keys.
- When listing files, always use `tree --gitignore` or append `-I 'node_modules|dist|build'` to your commands.
- Focus only on the source directories where the React and Tailwind code lives.
